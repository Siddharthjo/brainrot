# Brainrot — Workflows

**Status:** To be written as each milestone is implemented

---

## Will Contain

End-to-end workflow diagrams for the core loop:

```
Research → Knowledge Base → Ranking → Script → Approval → Production → Publishing → Analytics → Learning
```

And operational workflows:
- What happens when a provider API fails mid-pipeline (retry logic)
- What happens when a human rejects a script (feedback loop)
- What happens when analytics data arrives (learning loop trigger)

---

Last Updated: June 26, 2026
