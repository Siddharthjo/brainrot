# Brainrot — API Design

**Status:** To be written during M1 (Planning & Architecture)

---

## Will Contain

Full endpoint specification for every module, building on `docs/api/` subfolder docs (auth, channels, research, scripts, production, publishing, analytics).

Format per endpoint: method, path, auth requirement, request body, response body, error codes.

## Convention

API versioning is path-based (`/api/v1/...`) — see Decision #9.

---

Last Updated: June 26, 2026
